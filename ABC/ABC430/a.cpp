#include <bits/stdc++.h>
using namespace std;

int main(){
    int A, B, C, D;
    cin >> A >> B >> C >> D;
    C >= A ? (D >= B ? cout << "No" << endl : cout << "Yes" << endl) : cout << "No" << endl; 
}